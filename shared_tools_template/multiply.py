"""Drastically simplified multiply tool, with static typing"""

def execute(a: float, b: float) -> float:
    """Return the product of ``a`` and ``b`` without extra metadata."""

    return float(a) * float(b)
